from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Witha a given dataset, we are recommending a movie based on past data and we named as ZeeRecommenderSyatem',
    author='Sai',
    license='',
)
